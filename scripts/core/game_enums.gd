class_name GameEnums
extends RefCounted

enum ArtRarity {
	COMMON,
	UNCOMMON,
	RARE,
}

enum ArtCategory {
	ATTACK,
	SKILL,
	PASSIVE,
}

enum BattleSide {
	PLAYER,
	ENEMY,
}

enum BattlePhase {
	SETUP,
	PLAYER_TURN,
	ENEMY_TURN,
	VICTORY,
	FAILURE,
}

enum AttributeType {
	MAX_HEALTH,
	BASE_ATTACK,
	MAX_AP,
}

enum ModifierOperation {
	FLAT,
	ADDITIVE_PERCENT,
	MULTIPLICATIVE,
	OVERRIDE,
	CLAMP_MIN,
	CLAMP_MAX,
}

enum BuffStackingRule {
	REFRESH_DURATION,
	ADD_STACKS,
	REPLACE,
}

enum GridOccupantKind {
	UNIT,
	SCENE_OBJECT,
}

enum GridOperationCode {
	SUCCEEDED,
	INVALID_GRID,
	INVALID_OCCUPANT,
	NULL_TERRAIN,
	OUT_OF_BOUNDS,
	TERRAIN_BLOCKED,
	CELL_OCCUPIED,
	OCCUPANT_ALREADY_PLACED,
	OCCUPANT_NOT_FOUND,
	SOURCE_MISMATCH,
	DESTINATION_UNCHANGED,
}

enum GridPathStatus {
	FOUND,
	INVALID_GRID,
	OUT_OF_BOUNDS,
	DESTINATION_BLOCKED,
	NO_PATH,
}

enum BattlePlacementCode {
	SUCCEEDED,
	INVALID_BATTLE,
	BATTLE_NOT_IN_SETUP,
	INVALID_UNIT,
	OUT_OF_BOUNDS,
	TERRAIN_BLOCKED,
	CELL_OCCUPIED,
	UNIT_NOT_FOUND,
	GRID_STATE_REJECTED,
}

enum ActionFailureCode {
	NONE,
	INVALID_REQUEST,
	INVALID_BATTLE,
	INVALID_PHASE,
	BATTLE_NOT_ACTIVE,
	WRONG_TURN,
	ACTOR_NOT_FOUND,
	ACTOR_SIDE_MISMATCH,
	ACTOR_DEFEATED,
	ACTOR_NOT_PLACED,
	DESTINATION_OUT_OF_BOUNDS,
	DESTINATION_BLOCKED,
	DESTINATION_OCCUPIED,
	DESTINATION_UNCHANGED,
	NO_PATH,
	INSUFFICIENT_AP,
	ART_NOT_FOUND,
	ART_NOT_USABLE,
	ART_ON_COOLDOWN,
	INVALID_TARGET_SELECTION,
	ART_EXECUTION_UNAVAILABLE,
	CONDITION_FAILED,
	TARGET_OUT_OF_RANGE,
	TARGET_RELATION_INVALID,
	LINE_OF_SIGHT_BLOCKED,
	EFFECT_PLAN_INVALID,
	EFFECT_EXECUTION_FAILED,
	TRIGGER_LIMIT_EXCEEDED,
	UNSUPPORTED_ACTION,
	STATE_CHANGED,
	CONDITION_CONTEXT_INVALID,
	INTENT_GENERATION_FAILED,
	INTENT_EXECUTION_FAILED,
	SCROLL_NOT_FOUND,
	SCROLL_EMPTY,
	SCROLL_NOT_USABLE,
}

enum TargetRelation {
	SELF,
	ALLY,
	ENEMY,
	NEUTRAL,
	ANY,
}

enum TargetKind {
	CELL,
	UNIT,
	TERRAIN_OBJECT,
	BATTLE,
}

enum EffectTargetSource {
	ACTOR,
	HIT_UNITS,
	EVENT_SOURCE_UNIT,
	EVENT_TARGET_UNIT,
}

enum HitTargetKind {
	ANY,
	UNIT,
	SCENE_OBJECT,
}

enum BattleEventKind {
	UNIT_MOVED,
	DAMAGE_APPLIED,
	HEALING_APPLIED,
	SHIELD_CHANGED,
	BUFF_APPLIED,
	BUFF_REMOVED,
	UNIT_DEFEATED,
	ART_USED,
	TURN_STARTED,
	TURN_ENDED,
	BATTLE_ENDED,
	SCROLL_USED,
}

enum EventUnitRole {
	SOURCE,
	TARGET,
}

enum EventDataCapability {
	SOURCE_UNIT = 1,
	TARGET_UNIT = 2,
	SIDE = 4,
	ROUND = 8,
	ART = 16,
	BUFF = 32,
	POSITION = 64,
	BATTLE_PHASE = 128,
	SCROLL = 256,
}

enum ConditionContextKind {
	ART_INSTALL,
	ACTION_USE,
	EVENT_TRIGGER,
	ENEMY_DECISION,
	REWARD_GENERATION,
	MAP_EVENT,
}

enum SideRelation {
	SAME,
	OPPOSING,
}

enum TriggerSourceKind {
	ART,
	BUFF,
	RELIC,
}

enum BattleSourceKind {
	UNIT,
	RELIC,
	SCROLL,
	SYSTEM,
}

enum CardinalDirection {
	UP,
	RIGHT,
	DOWN,
	LEFT,
}

enum IntentKind {
	LOCKED,
	PATTERN,
	ENHANCE,
}

enum IntentTargetRule {
	SELF,
	NEAREST_OPPONENT_UNIT,
	NEAREST_OPPONENT_CELL,
	LOWEST_HEALTH_ALLY_UNIT,
	NEAREST_SCENE_OBJECT,
}

enum IntentMovementRule {
	NONE,
	TOWARD_TARGET,
}

enum IntentDirectionRule {
	FIXED,
	TOWARD_TARGET,
}

enum IntentSequence {
	ART_ONLY,
	MOVE_THEN_ART,
	ART_THEN_MOVE,
}

enum IntentStepStatus {
	EXECUTED,
	FIZZLED,
	SKIPPED,
	INTERNAL_FAILURE,
}

enum IntentExecutionStatus {
	COMPLETED,
	SKIPPED,
	INTERNAL_FAILURE,
}

enum NumericComparison {
	LESS_THAN,
	LESS_OR_EQUAL,
	GREATER_THAN,
	GREATER_OR_EQUAL,
}

enum ForcedMovementDirection {
	AWAY_FROM_ACTOR,
	TOWARD_ACTOR,
	FIXED,
}

enum ArtLoadoutCode {
	SUCCEEDED,
	INVALID_UNIT,
	INVALID_ART,
	INVALID_SLOT,
	SLOT_OCCUPIED,
	SLOT_EMPTY,
	MISSING_TAG,
	CONDITION_FAILED,
	NO_UPGRADE,
}

enum EnemyRank {
	STANDARD,
	ELITE,
	BOSS,
}

enum ConditionStatus {
	PASSED,
	FAILED,
	INVALID_CONTEXT,
}

enum EffectStatus {
	SUCCEEDED,
	FAILED,
	INVALID_CONTEXT,
	INVALID_DEFINITION,
	INVALID_TARGET,
	STATE_CHANGED,
}

enum DefinitionValidationCode {
	EMPTY_ID,
	INVALID_VALUE,
	NULL_REFERENCE,
	DUPLICATE_REFERENCE,
	INVALID_REFERENCE,
	INVALID_TAG,
	INVALID_INSTALL_CONDITION,
	INVALID_TARGETING,
	MISSING_EFFECT,
	MISSING_TRIGGER,
	INVALID_MODIFIER,
	INVALID_BUFF,
	UNSUPPORTED_DEFINITION,
	INVALID_INTENT,
	INVALID_ENEMY,
	INVALID_REWARD,
	INVALID_MAP,
	INVALID_ENCOUNTER,
	INVALID_MAP_EVENT,
}

enum RunPhase {
	READY,
	IN_BATTLE,
	CHOOSING_REWARD,
	SHOPPING,
	ENDED,
	PREPARING_BATTLE,
	RESOLVING_MAP_NODE,
}

enum RunEndReason {
	NONE,
	VICTORY,
	DEFEAT,
	ABANDONED,
}

enum RunCommandCode {
	SUCCEEDED,
	INVALID_RUN,
	INVALID_COMMAND,
	INVALID_PHASE,
	INVALID_AMOUNT,
	INSUFFICIENT_GOLD,
	TEAM_FULL,
	UNIT_NOT_FOUND,
	LAST_AVAILABLE_UNIT,
	ART_NOT_FOUND,
	ART_ALREADY_INSTALLED,
	ART_NOT_INSTALLED,
	INVALID_ART_SLOT,
	ART_RULE_REJECTED,
	RELIC_NOT_FOUND,
	RELIC_LIMIT_REACHED,
	SCROLL_NOT_FOUND,
	SCROLL_CAPACITY_EXCEEDED,
	INVALID_TARGET,
	OFFER_NOT_FOUND,
	OFFER_CLOSED,
	OPTION_NOT_FOUND,
	OPTION_UNAVAILABLE,
	BATTLE_SESSION_MISMATCH,
	OUTCOME_ALREADY_APPLIED,
	INVALID_BATTLE_OUTCOME,
	REWARD_GENERATION_FAILED,
	INTERNAL_FAILURE,
}

enum RewardRarity {
	COMMON,
	UNCOMMON,
	RARE,
}

enum RewardKind {
	CURRENCY,
	ART,
	RELIC,
	SCROLL,
	UNIT,
	HEALING,
	ART_UPGRADE,
}

enum RewardOfferRule {
	PICK_ONE,
	TAKE_ALL,
	PURCHASE_ANY,
	PICK_ANY,
}

enum RewardSource {
	BATTLE,
	SHOP,
	RECRUITMENT,
	CHEST,
	EVENT,
}

enum RewardOfferStatus {
	OPEN,
	COMPLETED,
	CLOSED,
}

enum RewardOptionStatus {
	AVAILABLE,
	CLAIMED,
	SOLD,
	CLOSED,
	SKIPPED,
}

enum RewardGenerationMode {
	STRICT,
	PROGRESSION_SAFE,
}

enum MapNodeKind {
	START,
	BATTLE,
	ELITE,
	BOSS,
	SHOP,
	CAMP,
	CHEST,
	EVENT,
}

enum MapNodeStatus {
	UNVISITED,
	ENTERED,
	RESOLVED,
}

enum MapSessionStage {
	PREPARING_BATTLE,
	IN_BATTLE,
	AWAITING_REWARD,
	SHOPPING,
	EVENT_CHOICE,
	EVENT_RESULT_PLANNED,
	AWAITING_EVENT_REWARD,
	FAILED,
}

enum MapEventStage {
	CHOOSING,
	RESULT_PLANNED,
	AWAITING_REWARD,
	COMPLETED,
}

enum MapFlowCode {
	SUCCEEDED,
	INVALID_RUN,
	INVALID_MAP,
	INVALID_DEFINITION,
	INVALID_PHASE,
	NODE_NOT_FOUND,
	NODE_NOT_REACHABLE,
	NODE_ALREADY_RESOLVED,
	SESSION_MISMATCH,
	INVALID_NODE_KIND,
	INVALID_DEPLOYMENT,
	EVENT_CHOICE_NOT_FOUND,
	EVENT_CHOICE_UNAVAILABLE,
	EVENT_RESULT_NOT_PLANNED,
	REWARD_FAILED,
	BATTLE_FAILED,
	STATE_CHANGED,
	INTERNAL_FAILURE,
}
