class_name GameEnums
extends RefCounted

enum ArtRarity {
	COMMON,
	UNCOMMON,
	RARE,
}

enum ArtCategory {
	ATTACK,
	SKILL,
	PASSIVE,
}

enum BattleSide {
	PLAYER,
	ENEMY,
}

enum BattlePhase {
	SETUP,
	PLAYER_TURN,
	ENEMY_TURN,
	VICTORY,
	FAILURE,
}

enum AttributeType {
	MAX_HEALTH,
	BASE_ATTACK,
	MAX_AP,
}

enum ModifierOperation {
	FLAT,
	ADDITIVE_PERCENT,
	MULTIPLICATIVE,
	OVERRIDE,
	CLAMP_MIN,
	CLAMP_MAX,
}

enum BuffStackingRule {
	REFRESH_DURATION,
	ADD_STACKS,
	REPLACE,
}

enum GridOccupantKind {
	UNIT,
	SCENE_OBJECT,
}

enum GridOperationCode {
	SUCCEEDED,
	INVALID_GRID,
	INVALID_OCCUPANT,
	NULL_TERRAIN,
	OUT_OF_BOUNDS,
	TERRAIN_BLOCKED,
	CELL_OCCUPIED,
	OCCUPANT_ALREADY_PLACED,
	OCCUPANT_NOT_FOUND,
	SOURCE_MISMATCH,
	DESTINATION_UNCHANGED,
}

enum GridPathStatus {
	FOUND,
	INVALID_GRID,
	OUT_OF_BOUNDS,
	DESTINATION_BLOCKED,
	NO_PATH,
}

enum BattlePlacementCode {
	SUCCEEDED,
	INVALID_BATTLE,
	BATTLE_NOT_IN_SETUP,
	INVALID_UNIT,
	OUT_OF_BOUNDS,
	TERRAIN_BLOCKED,
	CELL_OCCUPIED,
	UNIT_NOT_FOUND,
	GRID_STATE_REJECTED,
}

enum ActionFailureCode {
	NONE,
	INVALID_REQUEST,
	INVALID_BATTLE,
	INVALID_PHASE,
	BATTLE_NOT_ACTIVE,
	WRONG_TURN,
	ACTOR_NOT_FOUND,
	ACTOR_SIDE_MISMATCH,
	ACTOR_DEFEATED,
	ACTOR_NOT_PLACED,
	DESTINATION_OUT_OF_BOUNDS,
	DESTINATION_BLOCKED,
	DESTINATION_OCCUPIED,
	DESTINATION_UNCHANGED,
	NO_PATH,
	INSUFFICIENT_AP,
	ART_NOT_FOUND,
	ART_NOT_USABLE,
	ART_ON_COOLDOWN,
	INVALID_TARGET_SELECTION,
	ART_EXECUTION_UNAVAILABLE,
	CONDITION_FAILED,
	TARGET_OUT_OF_RANGE,
	TARGET_RELATION_INVALID,
	LINE_OF_SIGHT_BLOCKED,
	EFFECT_PLAN_INVALID,
	EFFECT_EXECUTION_FAILED,
	TRIGGER_LIMIT_EXCEEDED,
	UNSUPPORTED_ACTION,
	STATE_CHANGED,
	CONDITION_CONTEXT_INVALID,
}

enum TargetRelation {
	SELF,
	ALLY,
	ENEMY,
	NEUTRAL,
	ANY,
}

enum TargetKind {
	CELL,
	UNIT,
	TERRAIN_OBJECT,
	BATTLE,
}

enum EffectTargetSource {
	ACTOR,
	HIT_UNITS,
	EVENT_SOURCE_UNIT,
	EVENT_TARGET_UNIT,
}

enum HitTargetKind {
	ANY,
	UNIT,
	SCENE_OBJECT,
}

enum BattleEventKind {
	UNIT_MOVED,
	DAMAGE_APPLIED,
	HEALING_APPLIED,
	SHIELD_CHANGED,
	BUFF_APPLIED,
	BUFF_REMOVED,
	UNIT_DEFEATED,
	ART_USED,
	TURN_STARTED,
	TURN_ENDED,
	BATTLE_ENDED,
}

enum EventUnitRole {
	SOURCE,
	TARGET,
}

enum EventDataCapability {
	SOURCE_UNIT = 1,
	TARGET_UNIT = 2,
	SIDE = 4,
	ROUND = 8,
	ART = 16,
	BUFF = 32,
	POSITION = 64,
	BATTLE_PHASE = 128,
}

enum ConditionContextKind {
	ART_INSTALL,
	ACTION_USE,
	EVENT_TRIGGER,
}

enum SideRelation {
	SAME,
	OPPOSING,
}

enum TriggerSourceKind {
	ART,
	BUFF,
}

enum ArtLoadoutCode {
	SUCCEEDED,
	INVALID_UNIT,
	INVALID_ART,
	INVALID_SLOT,
	SLOT_OCCUPIED,
	SLOT_EMPTY,
	MISSING_TAG,
	CONDITION_FAILED,
	NO_UPGRADE,
}

enum EnemyRank {
	STANDARD,
	ELITE,
	BOSS,
}

enum ConditionStatus {
	PASSED,
	FAILED,
	INVALID_CONTEXT,
}

enum EffectStatus {
	SUCCEEDED,
	FAILED,
	INVALID_CONTEXT,
	INVALID_DEFINITION,
	INVALID_TARGET,
	STATE_CHANGED,
}

enum DefinitionValidationCode {
	EMPTY_ID,
	INVALID_VALUE,
	NULL_REFERENCE,
	DUPLICATE_REFERENCE,
	INVALID_REFERENCE,
	INVALID_TAG,
	INVALID_INSTALL_CONDITION,
	INVALID_TARGETING,
	MISSING_EFFECT,
	MISSING_TRIGGER,
	INVALID_MODIFIER,
	INVALID_BUFF,
	UNSUPPORTED_DEFINITION,
}
